# Legacy Target Reverse Prototype

This directory archives the old `src/target_reverse` prototype.

The active reverse target module is now:

- `src/reverse_target`
- `/reverse-target`
- `/api/reverse_target/...`

This legacy prototype is kept only for historical reference. It is not imported
by the current web routes or API layer.
