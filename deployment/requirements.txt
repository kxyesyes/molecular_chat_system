# MedChat CUDA 12.1 deployment profile.
# This profile and the root CI / CPU requirements are not interchangeable.
# MedChat server deployment dependencies
# Target environment: Linux + Python 3.10 + NVIDIA RTX3090 + CUDA 12.1 driver/runtime
#
# Install:
#   conda create -n medchat python=3.10 -y
#   conda activate medchat
#   pip install -r deployment/requirements.txt
#
# Notes:
# - AutoDock Vina, ADFRsuite and Ollama are external programs, not Python packages.
# - If RDKit fails through pip on your server, install it with:
#   conda install -c conda-forge rdkit -y
# - If FAISS GPU is required later, prefer conda:
#   conda install -c pytorch -c nvidia faiss-gpu -y

--extra-index-url https://download.pytorch.org/whl/cu121
--find-links https://data.pyg.org/whl/torch-2.4.0+cu121.html

# Web service
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20
websockets==14.1
aiofiles==24.1.0
jinja2==3.1.5

# Config, HTTP and utilities
pydantic==2.10.4
pyyaml==6.0.2
requests==2.32.3
httpx==0.28.1
tqdm==4.67.1
joblib==1.4.2

# Scientific computing
numpy==1.26.4
pandas==2.2.3
scipy==1.13.1
scikit-learn==1.5.2

# Cheminformatics
rdkit==2023.9.6
pubchempy==1.0.4
meeko==0.5.0

# RAG / vector retrieval
faiss-cpu==1.8.0.post1

# LLM / agent helpers used by the current codebase
langchain==0.2.16
langchain-core==0.2.43
langchain-community==0.2.17

# GPU deep learning stack for RTX3090 / CUDA 12.1
torch==2.4.0+cu121
torchvision==0.19.0+cu121
torchaudio==2.4.0+cu121
torch-geometric==2.6.1
torch-scatter==2.1.2+pt24cu121
torch-sparse==0.6.18+pt24cu121
torch-cluster==1.6.3+pt24cu121
torch-spline-conv==1.2.2+pt24cu121

# Optional local model / embedding utilities
transformers==4.44.2
accelerate==0.34.2
safetensors==0.4.5
