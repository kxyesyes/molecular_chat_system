# CI / CPU development profile (Python 3.10).
# This profile and deployment/requirements.txt are not interchangeable:
# the deployment profile pins a CUDA/PyG stack for the Linux GPU server.

# Web框架 - 核心依赖
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.6
websockets==12.0
aiofiles==23.2.1

# 数据处理 - 核心依赖
pandas==2.1.3
numpy==1.24.3
tqdm==4.66.1
scipy==1.11.4
scikit-learn==1.3.2
joblib==1.4.2

# 分子处理 - 核心功能
rdkit==2023.9.2
pubchempy==1.0.4

# 向量数据库 - RAG功能
faiss-cpu==1.7.4

# 工具和实用程序 - 必需
pydantic==2.5.0
pyyaml==6.0.1
jinja2==3.1.2
requests==2.31.0
httpx==0.25.2
pytest==7.4.3
pytest-asyncio==0.23.2

# GPU / 深度学习依赖请优先使用 deployment/requirements.txt。
# 不建议把 CUDA 版 torch/torch-geometric 写入根 requirements，
# 否则 Windows、本地 CPU 环境和服务器 CUDA 环境会互相冲突。

# 可选依赖（按需安装）
# langchain==0.0.339  # 如需高级LLM功能
# transformers==4.36.0  # 如需本地模型
# torch==2.1.0  # 如需深度学习功能
# chromadb==0.4.18  # 如需ChromaDB向量存储
# mordred==1.2.0  # 如需分子描述符计算
