# 分子聊天系统 (Molecular Chat System)

## 项目概述

分子聊天系统是一个面向药物设计与药物化学场景的智能 Web 平台，目标是把分子研发中的关键环节整合到同一套工作流中，实现从自然语言交互、知识检索、分子生成，到分子设计、分子对接、反向寻靶、活性预测和 ADMET 评估的端到端辅助分析。

项目以 Web 网站的形式提供服务，面向药物设计研究人员和药物化学专家。系统支持自然语言输入，能够输出文本结果、表格信息、分子结构相关内容以及可下载文件，适用于方案探索、候选分子筛选和研发流程自动化。

本项目当前处于毕业设计开发阶段。分子生成部分采用了自定义微调的 Llama3-8B 模型，使模型具备生成 SMILES 的能力；同时引入 Agent 思路，将多个工具和分析模块串联起来，提升药物设计任务的自动化程度与交互体验。

## 核心能力

- 智能对话问答，支持面向药物化学问题的自然语言交互
- RAG 检索增强生成，用于结合分子知识库进行辅助回答
- 分子生成与分子设计，支持基于大语言模型的 SMILES 输出
- 分子对接，用于评估候选分子与靶点的结合可能性
- 反向寻靶，用于探索潜在靶点与作用机制
- 活性预测，用于辅助筛选更有潜力的候选分子
- ADMET 预测，用于评估吸收、分布、代谢、排泄等性质

## 适用场景

- 药物分子初步设计与候选生成
- 分子性质分析与快速筛选
- 靶点相关的辅助检索与证据整理
- 药物研发流程中的多步骤自动化辅助

## 技术特点

- 采用 Web 架构，便于本地调试和后续服务器部署
- 使用 Ollama 作为本地大模型推理基础设施
- 结合 FAISS 与分子数据构建检索能力
- 模块化设计，便于扩展新的化学工具与智能体能力
- 面向药物设计任务进行了场景化整合，而非单一功能演示

## 快速开始

### 环境要求

- Python 3.10+
- Ollama 本地服务
- 建议具备 8GB 以上内存

### 安装依赖

```bash
pip install -r requirements.txt
```

### 启动项目

```bash
python main.py
```

启动后访问 `http://127.0.0.1:6001` 即可开始使用。端口、主机、worker 数等运行参数可通过 `.env` 或命令行覆盖；命令行参数优先级最高。

### 主模型接入与一次保存

首页默认使用 DeepSeek 官方 OpenAI 兼容接口 `https://api.deepseek.com/chat/completions`、
`deepseek-v4-pro`、流式输出；API Key 初始为空。在“模型接入设置”中填写自己的 Key 并保存，
同一本机、同一系统账号下重启、更新代码或切换工作树均会继续读取它。
接口/模型依据：[DeepSeek 官方文档](https://api-docs.deepseek.com/zh-cn/)。

- Windows：`%LOCALAPPDATA%/MedChat/config/llm.env`。
- Linux/其他系统：`${XDG_CONFIG_HOME:-~/.config}/medchat/llm.env`。
- 可通过 `MEDCHAT_USER_CONFIG_DIR` 选择仓库外的绝对目录（服务账号也需明确自己的配置目录）。

配置为本机私有明文文件，不上传 GitHub、不在公开 API 中返回 Key。POSIX 目录/文件权限为
0700/0600；Windows 检查目录/文件 owner 和 ACL，不允许不可信账号读写。权限不符会拒绝保存，
请选用当前账号的私有目录，不要降低权限检查。该机制不抵御已控制当前系统账号的进程。

密码框留空只会在相同服务商、相同接口地址时保留已保存 Key；换接口应填写对应的新 Key。
勾选“清除”可移除已保存 Key。保存成功不等于连接成功，请另外点击“测试连接”。
删除配置目录、重装系统、更换电脑或系统账号需要恢复配置；并非云端永久保存。

**兼容变化：** 首页不再隐式读取仓库 `.env`、旧 `scratch/llm_runtime_config.json`、
`MEDCHAT_LLM_CONFIG_PATH` 或 `OPENAI_COMPATIBLE_*` 等主模型环境设置，避免恢复历史接口/密钥。
已有的旧 Key 不自动迁移；首次升级需填写一次新 Key。独立验收 CLI 的显式环境变量接口仍保留，
端口、科学资产及 Ollama 配置不受影响。分子生成工具仍使用本地 `gmm-llama:latest`。

## 项目结构

```text
molecular_chat_system/
├── main.py                 # 主入口
├── requirements.txt        # Python 依赖
├── config/                 # 配置文件
├── data/                   # 数据与索引
│   ├── REGISTRY.md         # 本地数据资产登记与部署说明
│   └── samples/            # 轻量样例分子/蛋白文件
├── docs/                   # 文档
├── src/                    # 源代码
│   ├── agent/              # Agent 与工具层
│   ├── docking/            # 分子对接服务
│   ├── molecular_design/   # 分子设计服务
│   ├── rag/                # 检索增强生成
│   ├── reverse_target/     # 反向寻靶相关功能
│   ├── target_search/      # 靶点搜索与本地靶点库
│   ├── utils/              # 通用工具
│   └── web/                # Web 应用与路由
├── logs/                   # 日志
└── tests/                  # 测试
```

## 当前状态

本项目仍在持续开发中，当前重点是完善药物设计流程的自动化能力、提升模型输出质量，并逐步增强 Agent 协同能力。

## 许可证

本项目采用 MIT 许可证。
