# 分批集成 2：完整分子输入

基线：PR #7 合并后的 `32d30c8`。仅移入 `eb93c43` 中性质/类药性工具的完整输入解析，不携带源分支历史提交，不切换 Agent 主入口、不启用模型。

## 范围与兼容性

- 两个工具共享 `src/agent/tools/molecular_input.py`，先验证完整批次再计算，保留大小写、盐的点号、立体与同位素信息。
- 无效成员使整批失败，不提取有效片段、不静默丢弃；RDKit 缺失明确不可用。
- 推荐 `SMILES: CCO`、`SMILES: CCN` 分别一行，或用分号分隔。英文说明另起行。
- `SMILES: CCO.` 不再自动删除结构点号，这是有意收紧；名称/CXSMILES 扩展和空格分隔批次仍不支持。
- 不修改全局 BaseMolecularTool 或活性、生成、对接解析；不是通用自然语言解析器。
- 上一批性质报告测试取消结构选择替身，直接验证解析和真实 RDKit 计算。单独的阿司匹林 smoke 已由参数化真实数值测试覆盖。

## 验证

新输入测试在 main 基线复现 `58 failed, 8 passed`。独立审查进一步发现中文混合输入漏掉非法后缀、英文提示词被误认作分子；补 14 项失败回归后，以完整候选保留与受控说明词过滤修复。复审补 16 项失败回归，保留 ON/IN/OF 等大小写敏感结构，并识别 Cl/Br 开头的损坏字段。标签值仍严格校验，不从中删除说明词。

最终本地验证：输入/报告/提示词 168 passed；Agent 与反幻觉/健康检查 1688 passed、1 skipped（未启用性能测试）；contract 34/34；compileall 和 8 个 Node 脚本通过。集合有重叠，不累计。科学计算为真实本地 RDKit，不是外部模型或浏览器验收。合并前 CI 与复审结果以 PR 为准。

不提交真实数据、模型、凭据或运行输出；不覆盖原项目混杂工作树。后续活性代码与隔离 Agent 另批审查，合并代码不等于生产启用。
